# app-dcp-tracker

This app is initialized with:

- `CommunityToolkit.Maui` for MAUI helpers, behaviors, converters, and controls
- `CommunityToolkit.Mvvm` for observable properties and relay commands

The starter MVVM pattern lives in `DCPTracker/MainPage.xaml` and `DCPTracker/ViewModels/MainPageViewModel.cs`.